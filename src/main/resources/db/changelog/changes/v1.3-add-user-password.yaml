databaseChangeLog:
  - changeSet:
      id: 7
      author: liquibase
      comment: Add password column to users table
      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: password
                  type: varchar(255)
                  constraints:
                    nullable: false
